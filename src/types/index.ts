export type DataForm = {
  email: string;
  password: string;
};

export type Coin = {
  value: number;
  count: number;
};

export type Beverage = {
  name: string;
  price: number;
  count: number;
  img: string;
};

export type User = {
  email: string;
  password: string;
};
